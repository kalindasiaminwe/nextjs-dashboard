import { Merriweather, Lusitana } from 'next/font/google';

export const merriweather = Merriweather({
    subsets: ['latin'],
    weight: ["400", "700"]

});

export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: ["400", "700"]

});